#pragma once

#include <Core/Renderer.h>

// TAAU renderer wrapper (legacy core path, to be extracted later).
class TAAURenderer : public Renderer
{
public:
    TAAURenderer() = default;
    ~TAAURenderer() = default;
};

