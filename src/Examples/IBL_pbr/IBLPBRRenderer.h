#pragma once

#include <Core/Renderer.h>

// IBL-PBR renderer wrapper (legacy core path, to be extracted later).
class IBLPBRRenderer : public Renderer
{
public:
    IBLPBRRenderer() = default;
    ~IBLPBRRenderer() = default;
};

